#include <stdio.h>
#include "load.h"

int main() {
    ArrayDin arrayItems;
    TabInt arrayUsers;
    arrayItems = MakeArrayDin();
    MakeEmpty(&arrayUsers);
    
    printf("Masukkan command: ");
    StartWordNewLine;
    load(start)
}